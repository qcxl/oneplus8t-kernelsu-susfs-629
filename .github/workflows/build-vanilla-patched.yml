name: Build Vanilla Kernel (patched, olddefconfig)

on:
  push:
    branches: [ main ]
    paths:
      - 'kernel-patches/ksu.config.pure'
      - '.github/workflows/build-vanilla-patched.yml'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build vanilla kebab kernel
    runs-on: ubuntu-22.04
    container: ubuntu:20.04
    timeout-minutes: 180

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          apt-get update
          apt-get install -y \
            git curl wget zip unzip python3 python3-pip \
            build-essential libssl-dev bc cpio kmod ccache \
            device-tree-compiler lz4 zstd flex bison libelf-dev \
            gcc-aarch64-linux-gnu g++-aarch64-linux-gnu \
            gcc-arm-linux-gnueabi \
            lld llvm binutils

      - name: Clone Android clang r450784d toolchain
        run: |
          git clone --depth 1 https://github.com/TheBizarreAbhishek/clang-r450784d.git /tmp/clang-r450784d
          ls -lh /tmp/clang-r450784d/bin/clang
          /tmp/clang-r450784d/bin/clang --version

      - name: Clone LineageOS 20 kernel source (device exact commit)
        run: |
          git clone --depth 500 --branch lineage-20 \
            https://github.com/LineageOS/android_kernel_oneplus_sm8250.git kernel
          cd kernel
          git checkout 5dea892fe7e4
          echo "=== HEAD at commit ==="
          git log --oneline -1

      - name: Apply kernel compatibility patches
        working-directory: kernel
        run: |
          # fix_strnstr_msm_drv: strnstr() compat for kernel 4.19
          patch -p1 --forward --fuzz=3 < ../kernel-patches/fix_strnstr_msm_drv.patch || true
          # fix_dtb_no_qcom: NOT NEEDED - vendor/Makefile already excludes qcom (OPLUS custom)
          # fix_dup_techpack_display: REPLACED by fix_dup_techpack_only_dups.patch below
          # fix_dup_techpack_only_dups: remove 7 .o duplicates, keep 3 unique techpack files
          patch -p1 --forward --fuzz=3 < ../kernel-patches/fix_dup_techpack_only_dups.patch || true
          echo "=== Patches applied ==="

      - name: Configure kernel (PURE ORIGINAL CONFIG + debug)
        working-directory: kernel
        run: |
          export PATH="/tmp/clang-r450784d/bin:/usr/lib/ccache:$PATH"
          export ARCH=arm64
          export LLVM=1
          export LLVM_IAS=1

          CFG="CC=clang LD=ld.lld AR=llvm-ar NM=llvm-nm \
            OBJCOPY=llvm-objcopy OBJDUMP=llvm-objdump STRIP=llvm-strip \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            CROSS_COMPILE_COMPAT=arm-linux-gnueabi-"

          make O=out ARCH=arm64 $CFG vendor/kona-perf_defconfig

          cp ../kernel-patches/ksu.config.pure out/.config

          # Merge debug config (pstore/ramoops + verbose boot logging)
          # Device tree has ramoops@b0000000, size 4MB
          if [ -f ../kernel-patches/debug.config ]; then
            scripts/kconfig/merge_config.sh -m -O out \
              out/.config ../kernel-patches/debug.config
          fi

          make O=out ARCH=arm64 $CFG olddefconfig

          echo "=== Config ready, debug + ramoops enabled ==="

      - name: Save .config for debug
        working-directory: kernel
        run: cp out/.config /tmp/kernel-config.txt

      - name: Build kernel
        working-directory: kernel
        run: |
          export PATH="/tmp/clang-r450784d/bin:/usr/lib/ccache:$PATH"
          export ARCH=arm64
          export LLVM=1
          export LLVM_IAS=1

          make -j"$(nproc --all)" O=out ARCH=arm64 \
            CC=clang \
            LD=ld.lld \
            AR=llvm-ar \
            NM=llvm-nm \
            OBJCOPY=llvm-objcopy \
            OBJDUMP=llvm-objdump \
            STRIP=llvm-strip \
            CLANG_TRIPLE=aarch64-linux-gnu- \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            2>&1 | tee build.log

      - name: Verify kernel image
        working-directory: kernel
        run: |
            IMAGE_GZ=out/arch/arm64/boot/Image.gz
            IMAGE=out/arch/arm64/boot/Image
            if [ ! -f "$IMAGE_GZ" ] && [ ! -f "$IMAGE" ]; then
            echo "Build failed — no kernel image produced"
            tail -200 build.log
            exit 1
            fi
            if [ ! -f "$IMAGE_GZ" ] && [ -f "$IMAGE" ]; then
            echo "Image.gz not found, compressing Image..."
            gzip -c "$IMAGE" > "$IMAGE_GZ"
            fi
            ls -lh out/arch/arm64/boot/

      - name: Prepare artifact
        run: |
            mkdir -p artifact
            cp kernel/out/arch/arm64/boot/Image.gz artifact/Image.gz
            cp kernel/out/arch/arm64/boot/dtb.img artifact/dtb.img 2>/dev/null || true
            cp kernel/out/arch/arm64/boot/dtbo.img artifact/dtbo.img 2>/dev/null || true
            cp /tmp/kernel-config.txt artifact/config.txt 2>/dev/null || true
            # Include build-boot-img.py for local boot.img packaging
            cp build-boot-img.py artifact/
            cd artifact
            zip -r9 ../kebab-kernelsu-vanilla-a13-4.19.zip .
            cd ..
            ls -lh kebab-kernelsu-vanilla-a13-4.19.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: kebab-kernelsu-vanilla-a13-4.19
          path: kebab-kernelsu-vanilla-a13-4.19.zip
          retention-days: 30
